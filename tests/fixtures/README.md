# Scanner fixtures

These directories contain static inspection inputs. They are not production deployments
and their dependency versions are not recommendations. Lockfiles and workflows are
minimal detection indicators, not installable/working artifacts. Runtime tests use
temporary Git repositories. Synthetic secret strings are constructed in tests rather
than stored as real credentials.
