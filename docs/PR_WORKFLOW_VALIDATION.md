# Pull Request Workflow Validation

This repository-maintenance validation confirms that:

- Contribution branches can be created and pushed.
- Pull requests can be opened against `main`.
- GitHub CI triggers for pull requests.
- Maintainers can validate the contribution workflow end to end.

It does not modify production functionality.
